// This file kept intentionally small to avoid duplicate `bayar()` definitions.
// Payment logic is implemented in `app.js` which owns the cart and checkout flow.
// Keep a tiny sanity log so it's clear the file loaded.
console.log("script.js loaded - payment logic consolidated in app.js");
